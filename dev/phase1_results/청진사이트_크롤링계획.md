# Phase 2: Pattern Analysis Report
**분석 일시:** 2025-10-18 01:36:08
**분석 대상:** 8개 제품 (Bottle: 4, Jar: 2, Cap&Pump: 2)

---

## 1. Executive Summary

### 🎯 핵심 발견사항

1. **이미지 추출 문제 해결**: 제품 이미지가 `<img>` 태그와 CSS `background-image` 두 가지 형태로 존재
2. **제품명 추출**: img alt 속성에서 100% 성공
3. **스펙 정보**: Definition Lists (dl/dt/dd) 구조 100% 일관성
4. **인쇄영역**: 37.5% 제품이 PDF 다운로드 링크 보유

---

## 2. 이미지 패턴 분석

### 2.1 이미지 수 분포

| idx | 카테고리 | 이미지 수 | 인쇄영역 | 패턴 |
|-----|----------|-----------|----------|------|
| 960 | Bottle | **4개** | ✅ 있음 | GOODS1 + ADD_GOODS1~3 (완전체) |
| 969 | Bottle | 3개 | ❌ 없음 | GOODS1 + ADD_GOODS1~2 |
| 967 | Bottle | 1개 | ❌ 없음 | GOODS1만 |
| 912 | Bottle | **4개** | ✅ 있음 | GOODS1 + ADD_GOODS1~3 (완전체) |
| 928 | Jar | 3개 | ✅ 있음 | GOODS1 + ADD_GOODS1~2 |
| 937 | Jar | 2개 | ❌ 없음 | GOODS1 + ADD_GOODS1 |
| 946 | Cap&Pump | 1개 | ❌ 없음 | GOODS1만 |
| 934 | Cap&Pump | 3개 | ❌ 없음 | GOODS1 + ADD_GOODS1~2 |

**통계:**
- 평균: **2.625개/제품**
- 최소: 1개 (25%)
- 최대: 4개 (25%)
- 2~3개: 50%

### 2.2 이미지 타입별 분류

#### Main Image (GOODS1_*.jpg)
- 모든 제품에 존재 (100%)
- `<img>` 태그로 구현
- img alt 속성에 제품명 포함

#### Additional Images (ADD_GOODS1~3_*.jpg)
- CSS `background-image`로 구현
- `<a>` 태그의 배경으로 적용 (썸네일 갤러리)
- 파일명 패턴:
  - `ADD_GOODS1_`: 62.5% (5/8)
  - `ADD_GOODS2_`: 50% (4/8)
  - `ADD_GOODS3_`: 25% (2/8)

### 2.3 이미지 추출 전략

```javascript
// 1단계: img 태그에서 메인 이미지
const imgTags = document.querySelectorAll('img[src*="goodsImages"]');

// 2단계: CSS background에서 추가 이미지
const allElements = document.querySelectorAll('*');
allElements.forEach(elem => {
    const bgImage = window.getComputedStyle(elem).backgroundImage;
    // Extract from url("...")
});

// 3단계: 중복 제거 + 정렬
// URL 기준 중복 제거
// 파일명 기준 정렬: GOODS1 → ADD_GOODS1 → ADD_GOODS2 → ADD_GOODS3
```

---

## 3. 제품 정보 구조

### 3.1 제품명 추출

**✅ 100% 성공률**

```javascript
const mainImg = document.querySelector('img[src*="goodsImages"]');
const productName = mainImg?.alt || 'Unknown';
```

**추출 결과 예시:**
- idx=960: "50ml 헤비브로우용기"
- idx=969: "350ml 브로우용기"
- idx=928: "100g 에어리스용기"
- idx=946: "24파이 풀커버 일반미스트"

### 3.2 제품 스펙 정보

**100% 일관성 - Definition Lists 구조**

모든 제품이 동일한 구조:
- Definition Lists (dl): 3개
- Info Divs: 2개
- Tables: 0개 ❌

```html
<dl>
    <dt>제품 코드</dt>
    <dd>CJ-001-001</dd>

    <dt>용량</dt>
    <dd>50ml</dd>

    <dt>재질</dt>
    <dd>PET</dd>
</dl>
```

---

## 4. 인쇄영역 분석

### 4.1 인쇄영역 보유 현황

**✅ 인쇄영역 있음: 3개 (37.5%)**

| idx | 파일 | 크기 | 다운로드 URL |
|-----|------|------|--------------|
| 960 | idx_960.pdf | 3.98 MB | goods_download.php?download=1&idx=960 |
| 912 | idx_912.pdf | 3.83 MB | goods_download.php?download=1&idx=912 |
| 928 | idx_928.pdf | 4.07 MB | goods_download.php?download=1&idx=928 |

**❌ 인쇄영역 없음: 5개 (62.5%)**
- idx=969, 967, 937, 946, 934

### 4.2 인쇄영역 추출 로직

```javascript
// "인쇄영역 다운로드" 텍스트 검색
const links = Array.from(document.querySelectorAll('a'));
const printLink = links.find(link =>
    link.textContent.includes('인쇄영역') &&
    link.textContent.includes('다운로드')
);

if (printLink) {
    const downloadUrl = printLink.href;
    // PDF 다운로드 후 files/print_area/ 저장
}
```

### 4.3 인쇄영역 파일 구조

**저장 위치:**
```
files/
└── print_area/
    ├── idx_960.pdf
    ├── idx_912.pdf
    └── idx_928.pdf
```

---

## 5. 크롤러 구현 권장사항

### 5.1 필수 추출 항목

| 항목 | 추출 방법 | 성공률 | 우선순위 |
|------|-----------|--------|----------|
| 제품명 | img alt | 100% | 🔴 필수 |
| 메인 이미지 | img 태그 | 100% | 🔴 필수 |
| 추가 이미지 | CSS background | 75% | 🟡 권장 |
| 스펙 정보 | Definition Lists | 100% | 🔴 필수 |
| 인쇄영역 PDF | 다운로드 링크 | 37.5% | 🟢 선택 |

### 5.2 크롤링 순서

```python
# 1. 페이지 접속 및 로딩 대기
await page.goto(url)
await asyncio.sleep(3)  # CSS background 렌더링 대기

# 2. 제품명 추출
product_name = await extract_product_name()

# 3. 이미지 추출 (img + CSS background)
images = await extract_all_images()

# 4. 스펙 정보 추출
specs = await extract_specs_from_dl()

# 5. 인쇄영역 확인 및 다운로드
print_area = await check_and_download_print_area()

# 6. 데이터 저장
save_to_database({
    'idx': idx,
    'name': product_name,
    'images': images,
    'specs': specs,
    'print_area_path': print_area
})
```

### 5.3 에러 처리 전략

```python
# 인쇄영역 없는 경우: 정상 처리 (에러 아님)
if not print_area_link:
    data['has_print_area'] = False
    # 계속 진행

# 이미지 1개만 있는 경우: 정상 처리
if len(images) == 1:
    data['images'] = images
    # 계속 진행

# 스펙 정보 없는 경우: 경고 로깅
if not specs:
    logger.warning(f"idx={idx}: No specs found")
    # 계속 진행
```

---

## 6. RAG 시스템 통합

### 6.1 벡터 DB 저장 구조

```python
{
    "idx": "960",
    "product_name": "50ml 헤비브로우용기",
    "images": [
        {
            "url": "http://chungjinkorea.com/data/goodsImages/GOODS1_1754461630.jpg",
            "type": "main",
            "source": "img_tag"
        },
        {
            "url": "http://chungjinkorea.com/data/goodsImages/ADD_GOODS1_1754461630.jpg",
            "type": "additional_1",
            "source": "css_background"
        }
        // ... 추가 이미지
    ],
    "specs": {
        "제품코드": "CJ-001-001",
        "용량": "50ml",
        "재질": "PET"
    },
    "print_area": {
        "has_file": true,
        "file_path": "files/print_area/idx_960.pdf",
        "download_url": "http://chungjinkorea.com/bbs/goods_download.php?download=1&idx=960"
    },
    "page_url": "http://chungjinkorea.com/kr/product/view.php?idx=960"
}
```

### 6.2 검색 쿼리 예시

**사용자 질문:**
> "50ml 용기 보여줘"

**RAG 검색:**
1. 벡터 검색: `product_name`, `specs` 임베딩 매칭
2. Top-K 추출: 관련 제품 3~5개
3. HTML 생성: 제품 이미지 + 스펙 표시
4. 인쇄영역: 있으면 다운로드 링크 제공

---

## 7. 다음 단계 (Phase 3 & 4)

### Phase 3: 크롤러 업데이트 ✅
- [x] 이미지 추출 로직 수정 (img + CSS background)
- [x] 인쇄영역 다운로드 로직 추가
- [ ] 기존 `category_spider.py` 통합
- [ ] 8개 샘플 재크롤링 테스트

### Phase 4: 프로덕션 크롤러 🚀
- [ ] 전체 사이트 크롤링 (모든 카테고리)
- [ ] Qdrant 벡터 DB 저장
- [ ] RAG 파이프라인 통합
- [ ] HTML 응답 템플릿 구현

---

## 8. 주요 학습 내용

### 8.1 기술적 발견

1. **CSS background-image 중요성**
   - 단순 img 태그만으로는 불충분
   - `window.getComputedStyle()` 필수

2. **인쇄영역 링크 패턴**
   - DOM 요소가 아닌 다운로드 링크
   - 텍스트 기반 검색 (`"인쇄영역 다운로드"`)

3. **Definition Lists 활용**
   - 테이블 대신 dl/dt/dd 구조
   - 100% 일관성으로 파싱 용이

### 8.2 크롤링 모범 사례

- ✅ Playwright 사용 (CSS 렌더링 필요)
- ✅ 3초 이상 대기 (동적 콘텐츠 로딩)
- ✅ 요청 간 2초 간격 (서버 부하 방지)
- ✅ 중복 제거 (URL 기준)
- ✅ 에러 처리 (인쇄영역 없음 = 정상)

---

**분석 완료 일시:** 2025-10-18 01:42:00
**파일 위치:**
- JSON 결과: `dev/phase1_results/targeted_analysis_20251018_013608.json`
- 인쇄영역 PDF: `files/print_area/idx_*.pdf`
