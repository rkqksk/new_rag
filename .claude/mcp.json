{
  "mcpServers": {
    "filesystem": {
      "transport": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/home/rkqksk/projects/new_rag"
      ],
      "description": "Basic filesystem operations"
    },
    "serena": {
      "transport": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "export PATH=\"$HOME/.local/bin:$PATH\" && uvx --from git+https://github.com/oraios/serena serena start-mcp-server --context ide-assistant --project /home/rkqksk/projects/new_rag"
      ],
      "description": "Serena coding agent toolkit for semantic retrieval and editing"
    }
  }
}
