# ========================================
# RAG Enterprise Requirements
# Python 3.11+ Compatible (Latest Stable Versions)
# Updated: 2025-11-09
# ========================================

# FastAPI & ASGI Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20

# Pydantic V2
pydantic>=2.10.0
pydantic-settings>=2.7.0
email-validator>=2.2.0  # Email validation for Pydantic

# Database - PostgreSQL
psycopg2-binary>=2.9.10  # Legacy sync driver
psycopg[binary]>=3.2.0   # Modern async driver (psycopg3)
sqlalchemy>=2.0.36
asyncpg>=0.30.0

# Redis Cache (with async support for Python 3.11+)
redis[asyncio]>=5.2.0  # Async support built-in (replaces aioredis)
hiredis>=3.0.0  # C parser for performance boost

# Vector Database - Qdrant
qdrant-client>=1.12.0

# Analytics Stack (v6.0.0)
clickhouse-driver>=0.2.9  # ClickHouse OLAP database client
kafka-python>=2.0.2  # Kafka streaming platform
strawberry-graphql[fastapi]>=0.248.0  # GraphQL with FastAPI integration

# v7.0.0 - Open Source Production Stack
# Security & Authentication
python-keycloak>=3.9.0  # Keycloak OAuth2/OIDC client
hvac>=2.1.0  # HashiCorp Vault client
python-jose[cryptography]>=3.3.0  # JWT handling (already present)
PyJWT>=2.8.0  # v8.0.0 JWT token generation and verification
passlib[bcrypt]>=1.7.4  # v8.0.0 Password hashing with bcrypt

# Observability & Tracing
opentelemetry-api>=1.22.0  # OpenTelemetry API
opentelemetry-sdk>=1.22.0  # OpenTelemetry SDK
opentelemetry-instrumentation-fastapi>=0.43b0  # FastAPI auto-instrumentation
opentelemetry-instrumentation-requests>=0.43b0  # Requests instrumentation
opentelemetry-instrumentation-redis>=0.43b0  # Redis instrumentation
opentelemetry-instrumentation-psycopg2>=0.43b0  # PostgreSQL instrumentation
opentelemetry-exporter-jaeger>=1.21.0  # Jaeger exporter
opentelemetry-exporter-prometheus>=0.43b0  # Prometheus exporter

# Object Storage
minio>=7.2.3  # MinIO S3-compatible client
boto3>=1.34.0  # AWS SDK (S3 compatibility)

# ETL & Workflow
# NOTE: Airflow는 Docker로 실행 권장 (docker-compose.yml 참조)
# 로컬 설치 필요 시: pip install -r requirements-airflow.txt
# apache-airflow>=2.8.0  # OPTIONAL: Docker로 실행 (설치 무겁고 시간 오래 걸림)
# apache-airflow-providers-postgres>=5.10.0  # OPTIONAL

# v7.0.0+ - Realtime Backend (Convex-like, 100% Open Source)
python-socketio>=5.11.0  # Socket.IO server
python-socketio[asyncio]>=5.11.0  # Async support
# aioredis>=2.0.1  # REMOVED: Incompatible with Python 3.11 (TimeoutError conflict)
# Use redis[asyncio]>=5.2.0 instead - has async support built-in
watchdog>=4.0.0  # File system monitoring
psycopg2-pool>=1.1  # Connection pooling for LISTEN/NOTIFY

# ML & Embeddings
sentence-transformers>=3.3.0
torch>=2.5.0
transformers>=4.47.0
openai>=1.58.0  # OpenAI SDK for NexaAI compatibility

# v7.1.0 - Advanced Manufacturing (LORA + UR10e)
ultralytics>=8.3.0  # YOLOv8/v10 for vision inspection
peft>=0.12.0  # Parameter-Efficient Fine-Tuning (LORA adapters)
urx>=0.11.0  # Universal Robots URX library for UR10e control
numpy>=1.26.0  # Required for coordinate transformations

# v7.2.0 - Edge Computing Platform (Jetson + Raspberry Pi)
# Computer Vision Models
deep-sort-realtime>=1.3.2  # DeepSORT object tracking
mediapipe>=0.10.9  # MediaPipe (pose, hands, face mesh, holistic)
segment-anything>=1.0  # Meta's Segment Anything Model (SAM)
face-recognition>=1.3.0  # Face recognition with dlib (requires cmake)

# IoT & Sensors (Raspberry Pi GPIO/I2C/SPI)
RPi.GPIO>=0.7.1  # Raspberry Pi GPIO interface
smbus2>=0.4.3  # I2C bus access
spidev>=3.6  # SPI bus access
adafruit-circuitpython-dht>=4.0.4  # DHT temperature/humidity sensors
adafruit-circuitpython-bme280>=2.6.25  # BME280 environmental sensor
adafruit-circuitpython-mpu6050>=1.1.22  # MPU6050 accelerometer/gyro

# Industrial Protocols
paho-mqtt>=2.1.0  # MQTT client for IoT messaging
pymodbus>=3.6.8  # Modbus RTU/TCP protocol
opcua>=0.98.13  # OPC-UA industrial automation protocol
bleak>=0.22.0  # Bluetooth Low Energy (BLE) client

# Time Series & Analytics
psutil>=6.1.0  # System monitoring (CPU, memory, disk, sensors)
# timescaledb - PostgreSQL extension (installed via Docker)

# Audio Processing (for sound anomaly detection)
librosa>=0.10.2  # Audio analysis library
sounddevice>=0.5.1  # Audio recording/playback
scipy>=1.14.0  # Signal processing

# OCR & Image Processing (Latest Compatible Versions)
# Let pip resolve compatible versions automatically
paddleocr  # Latest version
paddlepaddle  # Latest stable
easyocr>=1.7.2
pytesseract>=0.3.13
opencv-python-headless  # Headless version for Docker (no GUI dependencies)
pillow>=11.0.0
PyMuPDF  # Latest version (pymupdf)
pandas>=2.2.0
openpyxl>=3.1.5
scikit-image>=0.24.0

# Monitoring & Logging
prometheus-client>=0.21.0
python-json-logger>=3.2.1

# HTTP & Async
httpx>=0.28.0
aiohttp>=3.11.0
requests>=2.32.0
beautifulsoup4>=4.12.3
lxml>=5.3.0

# Web Crawling & Automation
playwright>=1.48.0
selenium>=4.27.0

# Authentication & Security
pyotp>=2.9.0
requests-oauthlib>=2.0.0
python-jose[cryptography]>=3.3.0  # JWT token handling
passlib>=1.7.4  # Password hashing
stripe>=11.7.0  # Stripe payment processing

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# Development Tools
black>=24.10.0
ruff>=0.8.0
mypy>=1.13.0

# Utilities
python-dotenv>=1.0.1

# v7.3.0 - Professional Enterprise System
# Advanced RAG
rank-bm25>=0.2.2  # BM25 algorithm for hybrid search
# sentence-transformers already included above

# Distributed Web Crawling
# beautifulsoup4, lxml, aiohttp already included above
# playwright already included above

# MES (Manufacturing Execution System)
# Uses existing pydantic, datetime, uuid - no additional packages needed

# v8.5.0-v8.6.0 - Phase 8-9 Advanced Infrastructure
# Analytics & Monitoring
prometheus-client>=0.20.0  # Prometheus metrics collection
sentry-sdk[fastapi]>=2.18.0  # Error tracking and monitoring

# Machine Learning & Math
numpy>=1.26.0  # Numerical computing for recommendations

# Real-time WebSocket (already included in fastapi/uvicorn but explicitly listed)
# websockets>=12.0 - included with uvicorn[standard]
